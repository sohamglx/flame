import std.web
import pages

@Web
fn main() {
    @Layout
    fn AppLayout(children: Unknown) -> HtmlNode {
        return (
            <main>
                {children}
            </main>
        )
    }

    @Style
    fn styles() {"body {
        background-color: black;
        color: white;
    }"}
}
