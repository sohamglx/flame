import std.web
import pages

@Web
fn main() {
    @Layout
    fn AppLayout(children: Unknown) {
        <main>
            {children}
        </main>
    }
    @Style
    fn styles() {"body {
        background-color: black;
        color: white;
    }"}
}
